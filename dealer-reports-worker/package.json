{
  "name": "dealer-reports-backend",
  "version": "2.0.0",
  "description": "Seriti E-fficient API — Cloudflare Worker",
  "private": true,
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy",
    "secrets": "wrangler secret put SERITI_API_BASE_URL && wrangler secret put SERITI_API_KEY_ID && wrangler secret put SERITI_API_SECRET && wrangler secret put ALLOWED_ORIGINS && wrangler secret put WEBHOOK_SECRET"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
